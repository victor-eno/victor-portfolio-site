---

---

<div class="main-btn">
    <a class="btn one">See My Work</a>
</div>

<style>
    .btn {
    display: inline-block;
    padding: 6px 34px;
    text-align: center;
    border: 2px solid var(--blue-shift);
    font-family: 'Poppins', Courier, monospace;
    font-weight: 300;
    width: 300px;
    height: 45px;
    border-radius: 40px;
    cursor: pointer; 
    background-color: transparent;
    color: white;
    box-shadow: 5px 5px 5px var(--blue-shift);
}
    .btn {
    animation: blink 4s infinite alternate;
    }
    @-webkit-keyframes blink {
    40% {
        opacity: 1;
    }
    42% {
        opacity: 0.8;
    }
    43% {
        opacity: 1;
    }
    45% {
        opacity: 0.2;
    }
    46% {
        opacity: 1;
    }
}
    .one:hover {
    background: linear-gradient(45deg, #59befc, #48dbf8);
    color: white;
    border: 2px solid var(--blue-shift);
    background-position: 0%;
    box-shadow: 5px 5px 5px white;
    }
</style>